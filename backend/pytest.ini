[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = tests.py test_*.py *_tests.py
# --ds takes precedence over the DJANGO_SETTINGS_MODULE env var that .env sets
# for the container (which points at dev settings).
addopts = -ra -q --ds=config.settings.test
